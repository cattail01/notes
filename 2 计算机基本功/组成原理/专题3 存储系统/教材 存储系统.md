[toc]

### 第三章 存储系统

#### 3.1 存储系统概述

##### 1. 存储系统的层次结构

* 程序的局部性原理：时间局部性、空间局部性
* 多级存储系统的组成：内存储器、辅助存储器、外存储器、cache

##### 2. 存储器的分类

* 存储位元
* 存储介质：半导体、磁性材料和光存储器
* 存取方式：随机存取存储器、顺序存储存储器、半顺序存取存储器
* 读写功能：只读存储器ROM、随机存储器RAM
* 信息易失性：易失性存储器、非易失性存储器
* 与cpu的耦合程度

##### 3. 存储器的编制和端模式

* 字存储单元、字地址；字节存储单元、字节地址
* 端模式、大端模式、小端模式

##### 4. 存储器的性能指标

* 存储容量、存储时间、存储周期、存储器带宽

#### 3.2 静态随机存取存储器（SRAM）

* 特点：存取速度快，但是容量和密度不如DRAM大

##### 1. 基本的静态存储单元阵列

##### 2. 基本的SRAM逻辑结构

##### 3. SRAM读写时序

##### 4. 存储器容量扩充（重要）

* 存储表示基本结构：字数 * 位数

* 位扩展：两个存储器同时工作，输出数据并行输出
* 字扩展：用译码器让芯片产生高位和低位之分，分时工作
* 字位扩展：先位扩展，再字扩展

#### 3.3 动态随机存储器

##### 1. DRAM存储元的工作原理

* 常用作主存，组成（mos晶体管和电容）
* 读出过程破坏了电容上存储的信息，所以需要刷新，所以该存储器是易失性的
* DRAM的单元结构

##### 2. DRAM芯片的逻辑结构

##### 3. DRAM的逻辑时序

##### 4. DRAM的刷新操作

* 刷新周期、集中式刷新策略、分散式刷新策略

##### 5. 突发传输模式

##### 6. 同步DRAM（SDRAM）

##### 7. 双倍数据率（DDR SDRAM）

##### 8. CDRAM

* 附带高速缓冲存储器

#### 3.4 只读存储器

* 掩膜ROM、一次性编程ROM、电可擦ROM（E^2^rom）、闪存（FLASH）、NOR闪存、NAND闪存

#### 3.5 并行存储器

##### 1. 双端口存储器

* 冲突：双写，左右两端一读一写

##### 2. ==多模块交叉存储器==

* 顺序方式（原理、优点）交叉方式（原理、优点）
* 存储器模块两种交叉方式图像
* ==交叉存取度计算==
* ==计算带宽==

#### 3.6 cache存储器（重要）

##### 1. cache基本原理

* SRAM、对程序员透明
* cache和cpu、主存之间数据交换的单位（字、块）、cache命中、cache缺失
* ==cache的命中率==：命中率（h）、cache/主存系统的平均访问时间（ta）、访问效率计算

##### 2. 主存与cache的地址变换

* 全相联映射方式：原理，检索方法、优缺点
* 直接映射方式：原理、检索方法、优缺点
* 组相联映射方式：全相联与直接映射的整合

* v路组相联（一组内有v行）

##### 3. cache的替换策略

* 最不经常使用（LFU）算法、近期最少使用（LRU）算法、随机替换

##### 4. cache的写操作策略

* 写回法
* 全写法
* 写一次法

#### 3.7 虚拟存储器

##### 1. 虚拟存储器的基本概念

* 逻辑地址、物理地址、程序再定位、
*  cache与虚存的异同

##### 2. 页式虚拟存储器

* 逻辑页、物理页
* 每个进程对应一个页表
* 二级页表、反向页表
* 块表（TLB），块表与cache协同操作（TLB、页表与cache的关系）

##### 3. 段式存储器和段页式存储器

